{
  "name": "@webergency-utils/client",
  "version": "1.0.0",
  "type": "module",
  "description": "A fetch-based HTTP client with instance defaults, nested querystrings, form encoding, an opt-in cookie jar, and typed HTTP errors.",
  "author": "radixxko",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {},
  "main": "dist/client.cjs",
  "module": "dist/client.js",
  "types": "dist/client.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/client.d.ts",
        "default": "./dist/client.js"
      },
      "require": {
        "types": "./dist/client.d.cts",
        "default": "./dist/client.cjs"
      }
    }
  },
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "test": "vitest run",
    "fuzz": "jazzer fuzz_client.cjs",
    "coverage": "vitest run --coverage",
    "clean": "rm -rf ./dist/*",
    "build": "npm run clean && tsup",
    "commit": "node -e \"const version = require('./package.json').version.trim(); require('child_process').execSync('git add . && git commit -m \\\"Version ' + version + '\\\"');\"",
    "version": "npm run build && npm publish --access public && npm run commit && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webergency-utils/client.git"
  },
  "devDependencies": {
    "@jazzer.js/core": "^4.0.0",
    "@types/node": "^26.0.0",
    "@vitest/coverage-v8": "^4.1.9",
    "tsup": "^8.5.1",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
